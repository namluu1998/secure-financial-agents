---
description: Recommend frontend design/UI skill sources and install links
argument-hint: "[frontend design task, UI/UX goal, or install question]"
---

Load the `frontend-design-sources` skill and recommend the best source skill for the user's frontend design or UI/UX need. Respond in Vietnamese by default unless the user explicitly requests another language.
